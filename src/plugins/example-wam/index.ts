// A placeholder for a real WAM. In production, compile DSP to WASM and register here.
export function registerExampleWam() {
  // Implement AudioWorkletProcessor + parameterDescriptors and postMessage channels
}
